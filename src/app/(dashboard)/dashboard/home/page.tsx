export default function DashboardHome() {
  return (
    <section className="animate-in fade-in-0 slide-in-from-bottom-1">
      <h1 className="text-2xl font-semibold tracking-tight">Welcome back.</h1>
      <p className="mt-2 text-muted-foreground">{`Welcome to Pickle — let's build your first tournament.`}</p>
    </section>
  );
}
